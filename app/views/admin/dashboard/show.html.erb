<h1>Dashboard</h1>

<%= link_to "Current Projects", admin_projects_path %>

<% if @demo_night == nil %>
  <p>No current demo night available.</p>
<% elsif @demo_night.votes.length > 0 %>
  <%= render partial: 'vote_talley', locals: {projects: @demo_night.projects} %>
<% else %>
  <p>No votes yet.</p>
<% end %>
