<div class="row">
  <div class="col l6 offset-l3 s10 offset-s1">
    <h1 class="center-align page-title">Rating for <%= @project.name %></h1>
    <%= form_for(@vote, url: project_vote_path(@project)) do |f| %>
      <div class="row center-align">

        <div class="col s12">
          <p>Does this presentation represent the student community?</p>
        </div>
        <div class="input-field col s12">
          <%= f.select(:representation, [1, 2, 3, 4, 5], selected: 3) %>
        </div>
        <div class="col s12">
          <p>Does this project demonstrate technical excellence (e.g. good practices evident, well tested, technically challenging)?</p>
        </div>
        <div class="input-field col s12">
          <%= f.select(:challenge, [1, 2, 3, 4, 5], selected: 3) %>
        </div>
        <div class="input-field col s12">
          <p>Is this project/presentation compelling (e.g. solves a real problem, has a great story, has a wow factor)?</p>
        </div>
        <div class="input-field col s12">
          <%= f.select(:wow, [1, 2, 3, 4, 5], selected: 3) %>
        </div>
        <div class="input-field col s12">
          <%= f.submit "Submit", class: "btn turing-btn" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
